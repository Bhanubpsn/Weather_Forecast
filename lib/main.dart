import 'package:bpsn/weather_forecast/weather_forecast.dart';
import 'package:bpsn/util/colors.dart';

//**************************************************************JSON Parsing**********************************************************

void main() => (runApp(const MaterialApp(
  home: weather_forecast(),
)));
